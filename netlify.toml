[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (React Router) resolve on refresh / deep link.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
